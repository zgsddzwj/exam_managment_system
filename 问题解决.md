# 问题解决指南

## ✅ 已解决的问题

### 1. http://localhost:8000 显示 Page Not Found
**问题原因**：Django根路径没有配置视图

**解决方案**：
- 已添加根路径API视图 (`backend/config/views.py`)
- 现在访问 http://localhost:8000 会显示API信息

**验证**：
```bash
curl http://localhost:8000
# 应该返回JSON格式的API信息
```

### 2. http://localhost:5173 无法访问
**问题原因**：Node.js版本过低（v16.14.0），Vite 7需要Node.js 20.19+

**解决方案**：
- 已使用nvm升级Node.js到v20.19.6
- 前端服务器应该可以正常启动

**验证**：
```bash
node --version  # 应该显示 v20.19.6
curl http://localhost:5173  # 应该返回HTML页面
```

## 🚀 当前服务器状态

### 后端服务器
- **状态**：✅ 运行中
- **地址**：http://localhost:8000
- **API根路径**：http://localhost:8000/
- **Django Admin**：http://localhost:8000/admin
- **API端点**：
  - 认证：http://localhost:8000/api/auth/
  - 班级：http://localhost:8000/api/classes/
  - 任务：http://localhost:8000/api/tasks/
  - 提交：http://localhost:8000/api/submissions/

### 前端服务器
- **状态**：应该正在运行（如果Node.js已升级）
- **地址**：http://localhost:5173

## 🔧 如果前端仍然无法访问

### 方法1：手动启动前端（推荐）
```bash
# 确保使用Node.js 20
source ~/.nvm/nvm.sh
nvm use 20

# 进入前端目录
cd frontend

# 启动服务器
npm run dev
```

### 方法2：检查端口占用
```bash
# 检查5173端口
lsof -ti:5173

# 如果被占用，停止进程
kill $(lsof -ti:5173)

# 重新启动
cd frontend && npm run dev
```

### 方法3：查看日志
```bash
# 查看前端日志
tail -f frontend.log

# 查看后端日志
tail -f backend.log
```

## 📝 快速测试

### 测试后端
```bash
# 测试API根路径
curl http://localhost:8000

# 测试认证端点（应该返回401，说明API正常）
curl http://localhost:8000/api/auth/profile/
```

### 测试前端
1. 打开浏览器访问：http://localhost:5173
2. 如果看到登录页面，说明前端正常
3. 如果无法访问，检查：
   - Node.js版本：`node --version`（应该是20.x）
   - 前端进程：`ps aux | grep vite`
   - 端口占用：`lsof -ti:5173`

## 🎯 下一步操作

1. **访问前端**：http://localhost:5173
2. **注册账号**：选择"教师"或"学生"角色
3. **开始使用**：按照"快速开始.md"中的步骤操作

## ⚠️ 注意事项

1. **Node.js版本**：确保使用Node.js 20.x
   ```bash
   source ~/.nvm/nvm.sh
   nvm use 20
   ```

2. **代码执行功能**：需要配置Judge0 API密钥
   - 注册：https://rapidapi.com/
   - 订阅Judge0 API
   - 在`backend/.env`中添加密钥

3. **数据库**：当前使用SQLite（开发环境）
   - 数据文件：`backend/db.sqlite3`
   - 生产环境建议使用PostgreSQL

## 🆘 仍然有问题？

1. 检查服务器进程：
   ```bash
   ps aux | grep -E "(manage.py|vite)"
   ```

2. 检查端口：
   ```bash
   lsof -ti:8000,5173
   ```

3. 查看日志：
   ```bash
   tail -50 backend.log
   tail -50 frontend.log
   ```

4. 重启服务器：
   ```bash
   # 停止所有
   pkill -f "manage.py runserver"
   pkill -f "vite"
   
   # 重新启动后端
   cd backend
   source ../venv/bin/activate
   python manage.py runserver &
   
   # 重新启动前端（确保Node.js 20）
   source ~/.nvm/nvm.sh
   nvm use 20
   cd frontend
   npm run dev &
   ```

